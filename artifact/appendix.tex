% GraphMend, CGO 2027 -- Artifact Appendix.
%
% Structure: ctuning AE template (ae-20190108.tex).
% Style/length: PyTorch 2 (ASPLOS '24) and Synthesis of Sorting Kernels
% (CGO '25), both compact and claim-anchored. Sized for TWO pages.
%
% Place BEFORE \bibliography.
% PLACEHOLDERS: {{REPO_URL}}, {{COMMIT}}, {{DOI}}.
% LABELS to map to the paper: tab:benchmark, fig:phi4, fig:throughput,
%   fig:overhead, sec:eval, sec:coldstart, sec:steadystate, sec:throughput,
%   sec:serving, sec:overhead.

\appendix
\section{Artifact Appendix}

\subsection{Abstract}

The artifact contains the GraphMend implementation, its three AST-level
transformations (Predicated Data-Dependent Control Flow, Graph-Epilogue
Deferred Side Effects, Predicated Trap Lowering), benchmark drivers for the 27
Hugging Face models of Table~\ref{tab:benchmark}, and scripts for graph-break
detection, correctness checking, full-graph capture, latency and throughput
measurement, profiler collection, and compiler-overhead measurement. It
reproduces the results of Section~\ref{sec:eval}.

Break elimination, correctness and full-graph capture are \emph{CPU-only}: no
GPU, no pretrained weights, and no network for 21 of the 27 models. The latency
and throughput results depend on the GPUs used in the paper and are provided as
scripts that measure on the reviewer's own hardware. A container pins the
software stack; \texttt{scripts/run\_quick.sh} gives a pass/fail functional
result in 10--20 minutes.

\subsection{Artifact check-list (meta-information)}

{\small
\begin{itemize}\itemsep0pt
  \item {\bf Algorithm:} three legality-guarded source-level transformations.
  \item {\bf Program:} GraphMend, in the Jaseci/Jac compiler; PyTorch 2.12.1;
    Transformers 4.52.4.
  \item {\bf Compilation:} upstream \texttt{jaseci-labs/jaseci} at
    \texttt{e2b6b9f4b} (jaclang 0.36.1) plus \texttt{patches/graphmend.patch}
    (203 files, 166 new); used from source, no runtime PyPI dependencies.
  \item {\bf Transformations:} \textsc{[Where]}, \textsc{[Defer]},
    \textsc{[Trap]}; enabled by \texttt{graphmend = true} (default) plus
    \texttt{graphmend\_claim\_imports = true} (\emph{not} default).
  \item {\bf Model:} the 27 models of Table~\ref{tab:benchmark}; 21 built
    offline from small configurations, 6 needing \texttt{trust\_remote\_code}.
  \item {\bf Data set:} none; synthetic tensors seeded with
    \texttt{manual\_seed(0)}.
  \item {\bf Run-time environment:} Linux or macOS, Python 3.13; container
    images provided for CPU and GPU.
  \item {\bf Hardware:} any x86-64/arm64 CPU with \textbf{20\,GB of memory} for
    C1 and C2; an NVIDIA GPU for C3 (paper: RTX
    3090, A40, H100; CUDA 12.6, Triton 3.7).
  \item {\bf Run-time state:} cold-cache sensitive; each arm gets a private
    TorchInductor cache so cold start is genuinely cold.
  \item {\bf Execution:} \texttt{scripts/run\_quick.sh}, then
    \texttt{scripts/run\_full.sh}.
  \item {\bf Metrics:} graph-break count (FX graphs $-1$); fix rate; output
    fingerprint equality; full-graph capture; cold-start and steady-state
    latency; CUDA-graph launches per forward; throughput; compiler overhead.
  \item {\bf Output:} a table per experiment on stdout; each script prints the
    claim it checks and the expected result. Reference output in
    \texttt{artifact/README.md}.
  \item {\bf Experiments:} C1, break repair and behaviour preservation over 21
    offline and 6 network model rows; C2, full-graph capture; C3, GPU latency
    and throughput. Break-cause reporting and compiler overhead are provided
    but are not claims.
  \item {\bf Disk:} 35\,GB: 135\,MB for a clone, 1.4\,GB for the CPU image
    (7.3\,GB for the CUDA image), build cache, and the network rows' weights.
  \item {\bf Time to prepare:} $\approx$ 5 minutes (container build).
  \item {\bf Time to complete:} 10--20 minutes functional; 1.5--3 hours for the
    21-row sweep on a cold cache; 30--60 minutes for the GPU experiments.
  \item {\bf Publicly available?:} yes, {{REPO\_URL}} at {{COMMIT}}.
  \item {\bf Code licenses:} MIT.
  \item {\bf Archived (DOI)?:} {{DOI}}.
\end{itemize}
}

\subsection{Description}

\subsubsection{How to access}
Repository {{REPO\_URL}} at commit {{COMMIT}}, cloned with
\texttt{-{}-recurse-submodules}; archived at {{DOI}}. The compiler is not
vendored: \texttt{jaseci/} is the upstream toolchain frozen at a commit and
\texttt{patches/graphmend.patch} is everything GraphMend adds, so the patch is a
direct statement of the contribution. \texttt{scripts/setup.sh} verifies the
pin, applies the patch and fetches the typeshed stubs; it is idempotent, and the
archived copy is self-contained.

\subsubsection{Hardware dependencies}
Any x86-64 or arm64 CPU for C1 and C2. \textbf{20\,GB of memory is required}:
GraphMend compiles the imported modeling code, and that is what sets the floor,
not the weights, with \texttt{grounding-dino-base} peaking above 11.7\,GB.
Less causes an out-of-memory kill. C3 needs one NVIDIA GPU; GraphMend is functional on any sufficiently
recent device, but reproducing the magnitudes in Table~\ref{tab:benchmark}
requires the paper's RTX 3090, A40 or H100 configurations.

\subsubsection{Software dependencies}
Python 3.13, PyTorch 2.12.1, Transformers 4.52.4, NumPy 2.4.6, torchvision
0.27.1, \texttt{git}, and Docker for the pinned path; NVIDIA drivers, CUDA 12.6
and Triton 3.7 for the GPU experiments. Versions are pinned exactly rather than
floored: a graph-break count is a property of what TorchDynamo splits on, so a
different PyTorch release can report a different number.

\subsubsection{Data sets and models}
No data set; inputs are synthetic tensors at fixed shapes, seeded so both arms
receive identical weights and inputs, with the shape printed per arm and a
mismatch flagged. The models are the 27 rows of Table~\ref{tab:benchmark}.

\subsection{Installation}

\begin{verbatim}
git clone --recurse-submodules {{REPO_URL}}
cd CGO_AE_GraphMend
docker build -f artifact/Dockerfile -t gm .
docker run --rm gm
\end{verbatim}

\noindent Or natively: \texttt{bash scripts/setup.sh}.

\subsection{Experiment workflow}

The functional workflow answers whether GraphMend works, on a CPU with no
network, and exits non-zero on failure:

\begin{verbatim}
bash scripts/run_quick.sh     # 10-20 minutes
\end{verbatim}

\noindent It runs \texttt{t5-small}
(\textsc{[Defer]}), \texttt{Phi-4-mini-instruct} (\textsc{[Where]}, the
Figure~\ref{fig:phi4} example), \texttt{longformer-base-4096} and
\texttt{clap-htsat-fused}. The last two are expected \emph{not} to reach zero;
a run that fixed them would indicate a problem, not a success.
\texttt{scripts/run\_full.sh} then runs every claim the hardware allows,
skipping the GPU experiments where no CUDA device is visible.

One property of the setup is worth stating, because getting it wrong yields a
clean run that silently measures nothing: GraphMend claims imported third-party
code only under an opt-in that is off by default, and the entry program must be
compiled by Jac rather than run under plain CPython. The shipped scripts handle
both; the README documents them for reviewers writing their own.

\subsection{Evaluation and expected results}

Each claim has one script, named for it.

{\small
\begin{itemize}\itemsep0pt
  \item \texttt{run\_break\_analysis.sh -{}-c1} $\rightarrow$ \textbf{C1},
    Table~\ref{tab:benchmark}. One two-arm run over 27 models. Expect
    \textbf{21 models fully repaired, 3 partially, 3 unrepaired}, 105 of 139
    breaks eliminated, every row matching Table~\ref{tab:benchmark}'s repair
    rate, and a bit-identical output on every row carrying a comparison.
    Behaviour preservation is load-bearing: a row whose arms disagree fails the
    run rather than counting as a reduction.
  \item \texttt{run\_break\_analysis.sh -{}-c2} $\rightarrow$ \textbf{C2},
    Section~\ref{sec:serving}. Expect \texttt{fullgraph=True} to fail before
    the transform and succeed after it on all 21 fully repaired rows. The check
    is asymmetric: a both-pass result is a failure. Partially repaired rows are
    excluded, since capture must fail for them either way.
  \item \texttt{run\_latency\_analysis.py} $\rightarrow$ \textbf{C3},
    Sections~\ref{sec:coldstart}--\ref{sec:steadystate}. Two stages: a 10-row
    sample by default, every row under \texttt{-{}-full}, at about four times
    the cost. The sample is chosen for coverage, not speed: every rule fires,
    every modality appears, and both ends of the agreement range are present.
    Neither arm runs the compiler --- the fixed arm is stock transformers with
    GraphMend's output copied in from \texttt{artifact/fixed\_models/} --- which
    is the paper's methodology and is why an arm takes seconds. Expect cold
    start in the single to low double digits, inversely related to batch size,
    and steady state within a few percent of $1.0\times$. Gate the sources
    first with \texttt{verify\_fixed.py}; regenerate them with
    \texttt{gen\_fixed\_models.py} to check they are compiler output.
  \item \texttt{run\_throughput.sh} $\rightarrow$
    Figure~\ref{fig:throughput}: a gain that tracks the number of CUDA-graph
    launches eliminated and shrinks as the batch grows.
  \item \texttt{run\_compiler\_overhead.sh} $\rightarrow$
    Figure~\ref{fig:overhead}: mean overhead near 11.5\% cold and 1.1\% cached.
\end{itemize}
}

\paragraph{Expected variation.} Deterministic results must match exactly:
break counts, CUDA-graph launch counts, output fingerprints and capture
outcomes. Timings vary with the machine, so cold start is validated against a
floor of $1.5\times$ on any CUDA device rather than an expected value, and
steady state and throughput are reported without a threshold, since a fixed
bound would fail an honest run on different hardware.

\paragraph{Absolute break counts.} Six rows are measured through
\texttt{gpu/bench.py}, which builds them at the precision and batch shape
Table~\ref{tab:benchmark} used, and they reproduce its counts exactly. The
reason the build matters is the BART family, whose overflow guard leads with
\texttt{dtype == torch.float16}: that conjunct is a static bool, so in FP32
Dynamo folds it away and the data-dependent breaks behind it do not exist,
giving 3 instead of 7. \texttt{grounding-dino} is not precision-gated but needs
its real config and a batch built by the model's own processor. Four rows read
below Table~\ref{tab:benchmark} in absolute count while matching its
\emph{Fixed(\%)} column, which is what the claim concerns.

\subsection{Experiment customization}

Rows run by name (\texttt{run\_break\_analysis.sh t5-small biogpt}), and
\texttt{GM\_GPU\_MODELS} selects a subset for the GPU scripts. Rules are ablated
with \texttt{graphmend\_disable}, taking any of \texttt{trap}, \texttt{where},
\texttt{defer}. To target a model outside the suite, add a \texttt{\_build()}
returning \texttt{(model, inputs)} and a registry entry, then inspect the
detected breaks with \texttt{python -m paper\_eval.run\_why <model> on}, which
prints each surviving break with its cause and source location. What a model
imports matters: GraphMend claims imported code bounded by top-level package,
so a model whose code lives in a package with a large transitive import surface
is far more expensive to transform than one inside
\texttt{transformers.models.*}.

\subsection{Notes}

Enabling the code path under test matters as much as enabling GraphMend.
\texttt{Phi-4-mini-instruct} takes the LongRoPE branch only when
\texttt{rope\_scaling.type} is \texttt{"longrope"}; \texttt{clap} needs
\texttt{enable\_fusion}; \texttt{longformer} needs a sequence length that is not
a multiple of the attention window; and \texttt{Florence-2}'s guard is dead code
in FP32. \texttt{stella-en-400M-v5} reproduces its 0\% row only on CUDA with
xformers, because its breaks live behind the unpadding path.

\subsection{Methodology}

\begin{itemize}\itemsep0pt
  \item \url{https://www.acm.org/publications/policies/artifact-review-badging}
  \item \url{http://cTuning.org/ae/submission-20201122.html}
  \item \url{http://cTuning.org/ae/reviewing-20201122.html}
\end{itemize}
