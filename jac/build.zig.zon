.{
    .name = .jac,
    .version = "0.30.0",
    .minimum_zig_version = "0.16.0",
    .fingerprint = 0x125cfde06c37a26a,
    // The launcher, the payload tool and their tests are Jac: the build has no
    // Zig dependencies. Zig is the bootstrap seed (bootstrap/fetch_pbs.zig) and
    // the C/C++ cross-compiler for the LLVM shim and the vendored runtimes.
    .dependencies = .{},
    .paths = .{
        "build.zig",
        "build.zig.zon",
        "bootstrap",
        "launcher",
    },
}
