# Django #
*.log
*.pot
*.pyc
__pycache__
db.sqlite3
media
migrations
venv

# Jac build & cache artifacts
.jac/
*.jir
*.jbc
# legacy native IR cache (superseded by .jac/cache/native)
.jac_ir/

# Distribution / packaging
.Python
play/
build/
develop-eggs/
/dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Others #
.coverage
.session
*.session.db
.DS_Store
.vscode
build
*Zone.Identifier
.DS_Store
parser.out
codegen_output*

*.rdb
node_modules
out.dot
out.txt

# Mypy files #
.mypy_cache*



# Jaclang session files
*.session


# session files
*.bak
*.dat
*.dir
*.log

# Zig build
.zig-cache/
zig-out/
/dist/
.pbs-build/
# Pinned bun fetched per target by the payload tool's fetch-bun subcommand
# (jaclang/dist/payload/) and bundled into the payload (never committed). See
# build.zig's fetch-bun step.
.bun-build/
jaclang/client/_bun/

# Vendored typeshed stdlib stubs are fetched at the pinned commit by the
# payload tool's fetch-typeshed subcommand (zig build fetch-typeshed), not
# committed -- only the PIN / TARBALL_SHA256 / LICENSE / PROVENANCE.md beside
# the stubs are tracked.
jaclang/vendor/typeshed/stdlib
.llvm-build/
# CI staging dir for a cache-restored prebuilt shim (setup-jac -Dshim-bin).
.shim-build/
# Persistent JIR precompile cache: mkpayload seeds site/_precompiled from it
# and writes the refreshed tree back (see build.zig --precompiled-cache).
.precompiled-build/
# Persistent deps-layer frame cache: mkpayload reuses the payload's compressed
# dependency layer across builds (see build.zig --layer-cache).
.payload-layers/
# Zig-built LLVMPY_* shim, placed in-tree per host platform (see build.zig).
libjacllvm.so
libjacllvm.dylib
jacllvm.dll
# Zig-built libjacpyembed desktop shim, placed in-tree per host platform.
libjacpyembed.so
libjacpyembed.dylib
jacpyembed.dll

# jaclang/lib/ is SOURCE, not build output. The `lib/` rule in the packaging
# section above is meant for Python build directories, but it matches this one
# too, and the toolchain's bootstrap manifest requires jaclib.jac at run time.
# A clone missing it fails every single jac command with
#
#   RuntimeError: bootstrap_manifest: seed module 'lib/jaclib.jac' does not
#   exist under .../jaclang
#
# In the upstream repository these files predate the ignore rule, so they stay
# tracked and the problem is invisible there. This repository was populated by
# copying a checkout, where the rule applied and silently dropped them.
!jaclang/lib/
